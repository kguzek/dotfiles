# vim: ft=zsh

# GitHub Dependabot rebase
gh pr list --state open --app dependabot --json number |
  jq 'sort_by(.createdAt) | reverse | .[].number' |
  xargs -L1 echo gh pr comment --body "@dependabot rebase"
